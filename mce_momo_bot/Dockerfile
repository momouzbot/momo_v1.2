# MCE Momo Bot — production image
FROM python:3.12-slim

WORKDIR /app

# Sistema kutubxonalari (asyncpg/psycopg kabi paketlar uchun kerak bo'lishi mumkin)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Railway $PORT o'zgaruvchisini avtomatik beradi
ENV PYTHONUNBUFFERED=1

CMD ["sh", "-c", "alembic upgrade head && python -m app.seed && uvicorn app.main:app --host 0.0.0.0 --port $PORT"]
